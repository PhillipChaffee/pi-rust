//! Factory functions mirroring upstream `src/api.ts`: facet and service
//! definition with the reserved `$chord.` prefix rules, remote service
//! binding construction, the replicated state factory, and the member
//! registry that classifies service implementations for remote exposure.
//! The crate root re-exports this module's surface as the crate's single
//! import point, mirroring upstream's `src/index.ts` export list.
